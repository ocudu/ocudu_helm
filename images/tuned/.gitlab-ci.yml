# SPDX-FileCopyrightText: Copyright (C) 2021-2026 Software Radio Systems Limited
# SPDX-License-Identifier: BSD-3-Clause-Open-MPI

spec:
  inputs:
    ocudu_path:
      type: string
      default: ocudu/ocudu
      description: "OCUDU repository path."
    ocudu_ref:
      type: string
      default: dev
      description: "OCUDU reference branch or tag."
---
include:
  - project: $[[ inputs.ocudu_path ]]
    ref: $[[ inputs.ocudu_ref ]]
    file: .gitlab/ci-shared/docker.yml

stages:
  - test
  - publish
  - manifest

variables:
  TUNED_NAME: tuned-agent

.docker-builder-tuned:
  extends: .docker-builder
  variables:
    CONTEXT: images/tuned
    VERSION: "${TUNED_VERSION}_${CT_VERSION}"
    BUILD_ARGS: "TUNED_VERSION=${TUNED_VERSION}"
    NAME: ${TUNED_NAME}
    MULTI_ARCH_BUILD: "true"
    REGISTRY_URI: ${CI_REGISTRY}/${CI_PROJECT_PATH}
    REGISTRY_ORG: ""
  parallel:
    matrix:
      - PLATFORM: ["arm64", "amd64"]

only-build-tuned:
  stage: test
  extends: .docker-builder-tuned
  rules:
    - if: $ON_MR
  variables:
    MODE: test

build-and-publish-tuned:
  stage: publish
  extends: .docker-builder-tuned
  rules:
    - if: $ON_DEFAULT_BRANCH
    - if: $ON_MR
      when: manual
      allow_failure: true
  variables:
    MODE: publish

docker manifest tuned:
  extends: .docker manifest
  stage: manifest
  variables:
    REGISTRY_URI: ${CI_REGISTRY}/${CI_PROJECT_PATH}
    REGISTRY_ORG: ""
    NAME: ${TUNED_NAME}
    VERSION: "${TUNED_VERSION}_${CT_VERSION}"
  needs:
    - job: build-and-publish-tuned
      optional: false
